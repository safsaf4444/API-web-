[build]
builder = "nixpacks"

[deploy]
# We use '&&' to ensure the app ONLY starts if migrations succeed
startCommand = "alembic upgrade head && python -m uvicorn backend.app:app --host 0.0.0.0 --port $PORT"