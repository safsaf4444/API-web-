[build]
builder = "nixpacks"

[deploy]
# Using double quotes outside and a plain number 8080
startCommand = "alembic upgrade head && uvicorn backend.app:app --host 0.0.0.0 --port 8080"

healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10