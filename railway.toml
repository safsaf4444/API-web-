[build]
builder = "nixpacks"

[deploy]
# Simplified to ensure a fast, clean start
startCommand = "alembic upgrade head && uvicorn backend.app:app --host 0.0.0.0 --port ${PORT:-8080}"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5